[workspace]
members = [
  "games/gomoku",
  "packages/nes",
  "packages/server",
  "packages/tauriapp"
]

exclude = ["deps"]

[profile.release.package.nes]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
debug = true