{
  "name": "@nesbox/tetris",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "start": "npm run build:dev -- --watch --servedir=dist",
    "build:dev": "esbuild src/index.ts --bundle --outdir=dist --target=es2020",
    "build": "npm run build:dev -- --minify && jszip-cli add --output dist dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@ffflorian/jszip-cli": "^3.1.9",
    "@nesbox/config": "^0.0.1",
    "@nesbox/sandbox": "^0.0.1",
    "esbuild": "^0.17.10"
  }
}
