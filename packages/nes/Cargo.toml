[package]
name = "nes"
version = "1.0.16"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
nesbox_utils = { path = "utils" }
tetanes = { path = "../../deps/tetanes", default-features = false }
